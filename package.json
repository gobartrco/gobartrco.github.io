{
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.11",
    "autoprefixer": "^10.4.21",
    "cssnano": "^7.1.0",
    "postcss": "^8.5.6",
    "postcss-cli": "^11.0.1",
    "postcss-scss": "^4.0.9",
    "tailwindcss": "^4.1.11"
  },
  "scripts": {
    "build": "npm run build:widgets && npm run postcss:build && bundle exec jekyll build",
    "build:widgets": "cd widgets/listings && npm install && npm run build",
    "start": "bundle exec jekyll serve --livereload",
    "postcss:build": "postcss _sass/main.scss -o assets/css/main.css --parser postcss-scss"
  },
  "dependencies": {
    "prettier": "^3.8.0"
  }
}
